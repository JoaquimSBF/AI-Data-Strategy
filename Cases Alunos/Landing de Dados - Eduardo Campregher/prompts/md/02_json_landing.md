# ETAPA 02 — JSON da landing

## COMO USAR ESTA ETAPA

Pasta do case: `Cases Alunos/Landing de Dados - Eduardo Campregher/`

1. Cole **este arquivo inteiro**.
2. Cole no `<<< >>>` os CSVs **ja limpos** (etapa 01), pasta `outputs/` — nao o `dados/` cru:

| Arquivo | Pasta |
|---------|--------|
| `metricas.csv`, `produtos.csv`, `canais.csv`, `textos_ui.csv`, `metas.csv` | `outputs/` |

Gere JSON: kpis_ultimo_mes, serie_mensal, mix_produto, mix_canal, i18n (so chaves existentes), vs_meta.
Proibido inventar mes, receita ou traducao. Nulo permanece null.

Material:
<<<
[COLE os CSVs de outputs/]
>>>

## ONDE SALVAR
- `outputs/02_landing.json` (obrigatorio)
- `outputs/02_landing.md`
